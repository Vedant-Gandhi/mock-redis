{
  "name": "redis-mock",
  "version": "0.1.0",
  "description": "This is an implementation of mock redis i.e an in memory redis for testing purposes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.0.3"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
